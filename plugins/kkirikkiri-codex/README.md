# kkirikkiri-codex

Codex marketplace package for the curated `kkirikkiri` workflow.

Included skills:
- `kkirikkiri` — Assembles a Codex-native agent team with shared memory and bounded delegation.

Packaging notes:
- This package includes only the Codex-native reference set and helper scripts needed at runtime.
- Non-runtime source notes are excluded from the packaged plugin.
